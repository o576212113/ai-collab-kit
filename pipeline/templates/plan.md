# <项目名> · 规划书

> **状态**:草案 → 审核员审 → **Owner 门禁②拍板** → 锁定(此后修改 = 硬升级)
> **上游宪法**:[需求书](requirements.md)(按 PIPELINE 存放约定,两份同放项目 `docs/` 目录)——本文件不得与需求书冲突,发现需求书没锁住的取舍 → 回决策问题清单补拍,不在这里私自定。
> **写法纪律**:锁"怎么搭",不写代码细节(那是模块规约的事);每个选型必须带"被否的选项 + 一句话理由"——没有对比的选型等于没选。

## 1. 架构一图流

<文字或 ASCII 把整体结构画清楚:几个部分、谁调谁、数据从哪流到哪。一屏放得下。>

## 2. 技术选型

| 选型点 | 选择 | 被否选项 + 一句话理由 | 重大? |
|---|---|---|---|
| 语言/运行时 | <X> | <Y:为什么不> | |
| <框架/存储/关键库> | <X> | <Y:为什么不> | <重大取舍标 ★,建议转议事环盲发再定> |

## 3. 模块切分

| 模块 | 职责(一句话) | 边界(明确不管什么) |
|---|---|---|
| M1 <名> | <干什么> | <不干什么/归谁> |

## 4. 建造序(按依赖排,先地基后上层)

```
M1 <地基,谁都依赖它> → M2 → M3(依赖 M1、M2)→ ...
```

<每个箭头一句话:为什么这个先。执行阶段按此序逐模块走阶段 2/3,不跳序;要跳 = 找 Owner。>

## 5. 风险探针清单(重型档先行强制;标准档列出后由 Owner 门禁②裁量哪些先验)

> 探针纪律(OPC POC 机制):探针只验证风险,不是提前开发;探针代码不直接沉淀为正式架构;探针结论(行/不行/换方案)记入决策账本。轻风险项目本节可写"无"。

| 风险假设 | 不成立的后果 | 探针怎么做(最小验证) |
|---|---|---|
| <例:ffmpeg 能在目标机型 30s 内合成 60s 视频> | 核心体验不成立,架构要推翻 | <写 50 行脚本实测一次> |

## 6. 数据与状态(标准档:一览;重型档:锁定)

**标准档**:核心数据实体一览(名字 + 一句话 + 大致字段)。

**重型档(锁定级,开码后修改 = 硬升级)**:

- 实体表:每实体的性质、生命周期、归属模块(OPC BRD 02 章精简版)
- 状态机:有状态流转的实体,画出状态与迁移规则,指定唯一权威模块(OPC BRD 03 章精简版)——**这两样开码后再改成本是灾难级,重型必须在此锁死**

## 7. Owner 决策问题清单(门禁②拍板材料)

> 起草纪律(与需求书 §7 同款):只列**产品级/硬升级级**取舍;技术实施细节(算法/库/内部 schema/索引/代码组织)在 §2 选型表自决留痕,**不进本清单**。Owner 逐条回答后,一个 Q 一条 D-NNN 誊录进决策账本。

| # | 问题 | 选项与代价 | AI 推荐 | Owner 拍板 |
|---|---|---|---|---|
| Q1 | <规划层的产品级取舍,如"离线优先还是联网优先"> | ... | ... | (待填) |

## 8. 重型扩展节(标准/轻型删除)

- 模块间接口契约约定(命名规则、谁 own 什么、双向配对原则)
- 非功能需求(性能门槛、离线要求、安全边界)
