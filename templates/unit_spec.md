# <单元名> · 开发规约(unit spec)

> **状态**:草案 → Reviewer 审 → **Owner 拍板(硬停点①)** → 锁死开码
> **纪律**:spec 先于代码;实现偏离 spec → 走变更审批(Owner),不在代码里擅改。
> **写法要求**:现状部分必须基于**亲读现有代码/文档**写成,禁止凭印象/凭训练知识虚构现状。

## §0 心智与红线(锁死)

- 本单元**是什么**:<一句话定位>
- 本单元**绝不是什么**:<逐条列出最容易漂过去的边界,如"只做展示,绝不触发执行">
- 不变式:<本单元必须守恒的系统级约束,如"凭证明文绝不出保险库进程">

## §1 范围(做什么 / 不做什么)

- 做:<FR 列表,每条一句话>
- 明确不做(诚实 scoping):<defer 到哪里、为什么>

## §2 硬升级对照(逐条过 _collab/escalation_rules.md)

| 类别 | 命中? | 说明 |
|---|---|---|
| 1~5 + 项目自定义 | <是/否> | <临近但未命中的也写出来,标"临近严守"> |

<任一命中 → 本 spec 必须先取得 Owner 对该项的拍板,并在此记录拍板结论。>

## §3 现状核实(亲读源码/文档)

<当前代码/文档里已有什么、缺什么、本单元建立在哪些既有件之上。给出文件路径与关键行号锚点(锚实现行,不锚注释)。>

## §4 接口与契约

<对外暴露什么、依赖什么;输入输出 schema;与相邻模块的边界谁 own 什么>

## §5 失败路径(fail-closed 设计)

<列出可能失败的场景,逐条写失败时的行为。原则:拿不到授权/数据/依赖时宁可拒绝服务,不可默认放行。>

## §6 增量切分

| 增量 | 内容 | 独立验证方式 |
|---|---|---|
| ① | <最小可审的第一步> | <测试/命令> |
| ② | ... | ... |

<每个增量必须独立可审、独立可验证;增量间 APPROVED 自动续跑(lean §四)。>

## §7 测试与验证策略

<单元测试覆盖哪些关键路径;回归测试必须让真实错误源真发生(不 mock 理想值喂自己);验证链命令。>

## §8 Owner 亲测点(硬停点②的脚本)

<模块完成后 Owner 亲手验收的具体步骤,写到可照抄执行:启动命令 → 操作序列 → 预期看到什么。>
