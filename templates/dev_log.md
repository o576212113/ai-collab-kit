# dev-log <单元名> — 实现(累积式,每增量追加一节)

> 读者是 Owner:写到不懂代码的人能跟上"项目发生了什么"。与代码同 commit 交付,缺失可被退回。

## 增量① — <一句话标题>

### 1. 这次做了什么

<列点,人话。改了哪些文件做了什么事。>

### 2. 为什么这么做

<关键取舍及理由;引用 spec 对应条目;被否掉的备选方案一句话带过。>

### 3. 怎么验证的

<命令 + 结果原文(测试 N passed / lint 干净 / 关键 grep 清零)。声称必须可复跑。>

### 4. Owner 怎么亲测

<可照抄执行的步骤:命令 → 操作 → 预期结果。>

### 5. 没完成什么(诚实 scoping)

<本增量明确没做的、defer 到后续增量/单元的。宁可长,不可漏。>

### 6. 风险与硬边界

<已知风险、临近的硬升级边界、后续增量需要警惕什么。>

---

## 增量② — <标题>

<同 6 段结构追加>
